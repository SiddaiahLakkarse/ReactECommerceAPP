import { useEffect, useState } from 'react';
import './App.css';
type Product={id:string;name:string;description:string;price:number;stockQuantity:number};
const apiUrl=import.meta.env.VITE_API_URL ?? 'http://localhost:5016/api';
export default function App(){const [products,setProducts]=useState<Product[]>([]); const [message,setMessage]=useState(''); useEffect(()=>{fetch(`${apiUrl}/products`).then(r=>r.ok?r.json():Promise.reject()).then(setProducts).catch(()=>setMessage('Start the API to load products.'))},[]); return <main><header><b>Northwind Market</b><span>Clean commerce, thoughtfully built</span></header><section className='hero'><small>CURATED GOODS / 2026</small><h1>Everyday essentials,<br/><em>beautifully considered.</em></h1><p>Discover products selected for quality, utility, and a little bit of joy.</p></section><section><div className='heading'><h2>Featured products</h2><span>{products.length} items</span></div>{message&&<p>{message}</p>}<div className='grid'>{products.map(p=><article className='card' key={p.id}><div className='image'>✦</div><div><h3>{p.name}</h3><p>{p.description}</p><strong>${p.price.toFixed(2)}</strong> <button onClick={()=>setMessage(`${p.name} added to your cart`)} disabled={!p.stockQuantity}>Add to cart</button></div></article>)}</div></section><footer>Northwind Market · Built with ASP.NET Core, React, and SQL Server</footer></main>}

